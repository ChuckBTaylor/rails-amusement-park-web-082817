<h1>List o' Attractions</h1>
<% if !user_is_admin? %>
  <ul>
    <% @attractions.each do |attraction| %>
      <li><%= link_to "Go on #{attraction.name}", attraction %></li>
    <% end %>
  </ul>

<% else %>
  <% @attractions.each do |attraction| %>
    <%= link_to "Show #{attraction.name}", attraction %>
  <% end %>
  <%= link_to "New Attraction", new_attraction_path %>
  <%=  %>
<% end %>
